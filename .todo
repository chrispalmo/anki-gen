1. delete row button
2. regenerate pinyin button
3. dark mode button

notes:

```
write a script that combines all ts, tsx, js, jsx files in a target directory into a single tsx file, and removes all imports

the script should take two arguments, one for the target directory, and one to specify where to save the output file (default to the current directory that the script is being run from).

respond with at least 5 questions to ensure you understand the requirements
```
